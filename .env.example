# Copia questo file in `.env` e riempi la chiave.
# Serve solo se usi EMBEDDER="openai" in config.py.
# Con i modelli locali (minilm-it, e5-small, bge-m3) NON serve alcuna chiave.
OPENAI_API_KEY=
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
